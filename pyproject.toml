[project]
name = "proxy-video-editor"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "itsdangerous",
    "jinja2",
    "psutil",
    "python-multipart",
    "uvicorn",
]
